// var assert = require("assert");
// var displayCount = require('../stopwatch.js').default

describe('Stopwatch', () => {
  describe('Startbutton', () => {
    it('Will one day run tests on the Startbutton', () => {
        // Setup
  
        // Exercise
  
        // Verify
      });

    it('Will contain a second Startbutton related test', () => {
      // Setup

      // Exercise

      // Verify
    });

    it('Will contain a third Startbutton related test', () => {
      // Setup

      // Exercise

      // Verify
    });   
  });
  
  describe('Stopbutton', () => {
      it('Will run tests on the stopButton', () => {
        // Stopbutton test 1
      });

      it('Will run a second test ont the stopButton', () => {
        // Stopbutton test 2
      });
  });
});